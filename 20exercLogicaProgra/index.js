//Escreva uma função chama ePaisagem que recebe dois argumentos, largura e altura de uma imagem (Number).
//Retorne true se a imagem estiver no modo paisagem.

const ePaisagem = (altura, largura) => altura < largura;

console.log(ePaisagem(102, 503))